name: Trigger Automation Pipeline

on:
  workflow_dispatch:
    inputs:
      branch_name:
        description: 'Branch name to create'
        required: true
        type: string
      target_folder_name:
        description: 'Target folder name (e.g., Customer-Infra-01)'
        required: true
        type: string

jobs:
  create-and-merge-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: Checkout test_branch_01
        run: |
          git fetch origin --quiet
          git checkout test_branch_01 2>/dev/null || git checkout -b test_branch_01 origin/test_branch_01
          git pull origin test_branch_01 --quiet
          echo "Checked out and updated test_branch_01"

      - name: Set up Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create or checkout branch
        run: |
          NEW_BRANCH="${{ github.event.inputs.branch_name }}"
          echo "NEW_BRANCH=${NEW_BRANCH}" >> $GITHUB_ENV
          git fetch origin --quiet
          git checkout ${NEW_BRANCH} 2>/dev/null || \
          git checkout -b ${NEW_BRANCH} origin/${NEW_BRANCH} 2>/dev/null || \
          git checkout -b "${NEW_BRANCH}"
          git pull origin ${NEW_BRANCH} 2>/dev/null || true
          echo "On branch: ${NEW_BRANCH}"

      - name: Copy template to target folder
        run: |
          TARGET_FOLDER="${{ github.event.inputs.target_folder_name }}"
          SOURCE_DIR="new_customer/Customer-Infra-Template"
          TARGET_DIR="new_customer/${TARGET_FOLDER}"
          
          if [ ! -d "${SOURCE_DIR}" ]; then
            echo "Source directory ${SOURCE_DIR} does not exist"
            exit 1
          fi
          
          if [ -d "${TARGET_DIR}" ]; then
            echo "Target directory ${TARGET_DIR} already exists"
            exit 1
          fi
          
          cp -r "${SOURCE_DIR}" "${TARGET_DIR}"
          echo "Copied ${SOURCE_DIR} to ${TARGET_DIR}"
          echo "TARGET_FOLDER=${TARGET_FOLDER}" >> $GITHUB_ENV
          echo "TARGET_DIR=${TARGET_DIR}" >> $GITHUB_ENV

      - name: Make changes
        run: |
          git add "${TARGET_DIR}"
          git commit -m "Add new folder ${TARGET_FOLDER} to $NEW_BRANCH"

      - name: Push new branch
        run: git push -u origin $NEW_BRANCH

      - name: Merge into test_branch_01 branch
        run: |
          git checkout test_branch_01
          git pull origin test_branch_01
          git merge --no-ff $NEW_BRANCH -m "Merge $NEW_BRANCH into test_branch_01"
          git push origin test_branch_01